# RMASmoke
RMASmoke is a buffer overflow in the Cr50 firmware's implementation of TPM2 (derived from Tss2)
This is what is being used to obtain the H1 BootROM.

Remove this directory from the repo and Makefile when we go public.